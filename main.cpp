#include <iostream>
#include <cstring>
using namespace std;

int main () {
  Heap heap;
  char choice;
  if (choice == "m") {
    //manual
  }

  else if (choice == "f") {
    //file
  }
  return 0;
}
